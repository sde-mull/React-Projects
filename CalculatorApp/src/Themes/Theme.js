
import { createTheme } from "@mui/material/styles"

const theme = createTheme({
	palette: {
	  background: { default: "#eeeeee", root: "#303030" },
	  primary: { main: "#E0E0E0", light: "#FFFFFF", screen: "#90a4ae" },
	  text: { primary: "#212121" }
	}
});

export default theme;